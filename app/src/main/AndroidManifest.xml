<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pis03_2016.savealltherobots">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/robot"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity
            android:name=".view.activity.SplashScreenActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.activity.MainActivity" />
        <activity
            android:name=".view.activity.GamePlayActivity"
            android:configChanges="orientation|keyboardHidden"
            android:label="@string/app_name" />
        <activity android:name=".view.activity.ScoreScreenActivity" />
        <activity android:name=".view.activity.HighscoreScreenActivity" />
        <activity android:name=".view.activity.SettingsScreenActivity" />
        <activity android:name=".view.activity.GameOverActivity" />
        <activity android:name=".view.activity.WelcomeScreenActivity" />
        <activity android:name=".view.activity.CreditsActivity"></activity>
    </application>

</manifest>
